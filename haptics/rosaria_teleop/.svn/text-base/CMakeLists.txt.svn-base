cmake_minimum_required(VERSION 2.8.3)
project(rosaria_teleop)

## Find catkin macros and libraries
find_package(catkin REQUIRED COMPONENTS roscpp geometry_msgs)

include_directories(${catkin_INCLUDE_DIRS})

catkin_package(
  INCLUDE_DIRS
#  LIBRARIES turtlebot_teleop
  CATKIN_DEPENDS roscpp geometry_msgs
  DEPENDS
)

###########
## Build ##
###########

#add_executable(turtlebot_teleop_joy src/turtlebot_joy.cpp)
#target_link_libraries(turtlebot_teleop_joy ${catkin_LIBRARIES})

add_executable(rosaria_teleop_keyboard src/rosaria_keyboard.cpp)
target_link_libraries(rosaria_teleop_keyboard ${catkin_LIBRARIES})


#############
## Install ##
#############

## Mark executable scripts (Python etc.) for installation
install(PROGRAMS
  scripts/cyber_glove_teleop.py
  DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
)

## Mark executables and/or libraries for installation
#install(TARGETS turtlebot_teleop_joy
#  RUNTIME DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
#)

## Mark all other useful stuff for installation
install(DIRECTORY launch
  DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION}
)
